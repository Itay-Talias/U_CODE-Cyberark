// import { createSlice } from "@reduxjs/toolkit";
// import { selectCount } from "../counter/counterSlice";
// import { selectRandomCount } from "../RandomCounter/RandomCountSlice";
// import { store } from "../../app/store";

// const count1 = store.getState().counter.value;
// const count2 = store.getState().randomCounter.value;

// const initialState = {
//     value: Math.max(count1, count2),
//     status: "idle",
// };

// export const MaxCounter = createSlice({
//     name: "maxCounterSlice",
//     initialState,
// });

// export const selectMaxCounter = (state) => Math.max(count1, count2);

// export const { increment, incrementOne } = MaxCounter.actions;

// export default MaxCounter.reducer;
