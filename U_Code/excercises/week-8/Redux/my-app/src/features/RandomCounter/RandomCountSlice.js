// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//     value: 0,
//     status: "idle",
// };

// export const RandomCountSlice = createSlice({
//     name: "randomCounter",
//     initialState,
//     reducers: {
//         increment: (state) => {
//             state.value += Math.random(0, 1);
//         },
//         incrementOne: (state) => {
//             state.value += 1;
//         },
//     },
// });

// export const selectRandomCount = (state) => state.randomCounter.value;

// export const { increment, incrementOne } = RandomCountSlice.actions;

// export default RandomCountSlice.reducer;
